{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@types/node": "^18",
    "nuxt": "^3.5.2"
  },
  "version": "0.0.0",
  "dependencies": {
    "@vuepic/vue-datepicker": "^5.4.0",
    "nuxt-storage": "^1.2.2"
  }
}
